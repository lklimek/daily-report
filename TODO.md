# TODO

- [ ] Use `--commenter` search as fallback for finding reviewed PRs that `--reviewed-by` misses (e.g. bot-authored PRs like tenderdash#1252 by Copilot are not indexed by `gh search prs --reviewed-by`)
